// DailyRoutineHelperView.js
import React from "react";
import { Container, Typography } from "@mui/material";

const DailyRoutineHelperView = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        📅 Daily Routine Helper
      </Typography>
      <Typography>
        Visual schedules and reminders to help children manage daily tasks and transitions with less stress.
      </Typography>
    </Container>
  );
};

export default DailyRoutineHelperView;
