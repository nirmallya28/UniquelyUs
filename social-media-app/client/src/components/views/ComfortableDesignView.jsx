import React from "react";
import { Container, Typography } from "@mui/material";

const ComfortableDesignView = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        🎨 Comfortable & Simple Design
      </Typography>
      <Typography>
        A sensory-friendly interface using calm colors and simple layouts to avoid overstimulation.
      </Typography>
    </Container>
  );
};

export default ComfortableDesignView;
